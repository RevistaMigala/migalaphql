export const tables = {
    responsesTable: process.env.FORM_RESPONSES_TABLE,
}

export const translations = {
    ConditionalCheckFailedException: 'El registro ya existe en la base de datos',
    default: 'Hubo un error inesperado',
}
